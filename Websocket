A websocket client library exists as [https://github.com/ahyatt/emacs-websocket a github repository].   It started out as part of the Google Wave client, and was recently split off into a new repository.

The library requires Emacs 24 to support wss (secure websockets).

----
CategoryExtensions
