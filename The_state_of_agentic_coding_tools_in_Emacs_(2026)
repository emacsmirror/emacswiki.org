== The State of Agentic Coding in Emacs — 2026 Comparison ==

This is a current-state comparison of Emacs AI coding tools and integrations. The goal is to distinguish full coding agents, editor frontends, protocol clients, autocomplete systems, and wrappers around external CLIs.

'''Legend'''

* '''Full''': can inspect a repository, edit multiple files, run commands/tests, and iterate.
* '''Editing''': project-aware multi-file editing, but not necessarily a complete autonomous command/test loop.
* '''Partial''': some agent-like behavior, often dependent on configured tools.
* '''Backend''': capability comes from the external CLI or agent.
* '''FIM''': fill-in-the-middle completion.
* '''NES''': Next Edit Suggestions.
* '''Automatic scope''': the tool can search/discover relevant repository files.
* '''Explicit scope''': the user normally supplies files, buffers, or context.

|| '''Tool''' || '''Agentic coding''' || '''Autocomplete / NES''' || '''Project-wide scope''' || '''Model / agent interaction''' || '''Multiple providers''' || '''Auth / billing''' || '''Remote / TRAMP''' || '''Persistent instructions / skills''' || '''Subagents / parallelism''' || '''Main limitations''' ||
|| '''[https://github.com/karthink/gptel gptel]''' || Partial through tool calling || No native continuous autocomplete; no true NES || Explicit by default; automatic with suitable tools || Emacs calls provider APIs directly; optional Elisp and MCP tools || Yes || Usually provider API keys or local/OpenAI-compatible endpoints || Remote buffers work, but shell/file tools require careful routing || System prompts, directives, project-local config || Not native; possible through tools/extensions || Not a standardized coding agent by default; capabilities vary heavily by user configuration ||
|| '''[https://github.com/karthink/gptel-agent gptel-agent]''' || Full || No native continuous autocomplete; no true NES || Automatic within the current project || Ready-made agent harness built on gptel presets and tools; gptel calls the selected provider API and can also use MCP tools || Yes, through gptel || Usually provider API keys or local/OpenAI-compatible endpoints supported by gptel || Remote buffers can be referenced, but filesystem and Bash tools must execute on the intended host || Built-in agent and planning presets; custom agents can be defined in Markdown or Org with their own prompts, tools, models, backends, and API parameters || Yes; context-isolated specialized subagents can run asynchronously or in parallel || Requires a current gptel version; agent workflows use more tokens; write, edit, Bash, and Emacs actions commonly require confirmation; remote execution needs careful configuration ||
|| '''[https://github.com/kmontag/macher macher]''' || Editing || No || Automatic project search/read || Uses gptel backends and project-editing tools || Yes, through gptel || Same as selected gptel backend || Works at the Emacs/project layer; remote execution may need care || Inherits gptel prompts and configuration || No native subagents || Strong controlled patch workflow, but not equivalent to an autonomous terminal agent repeatedly running tests ||
|| '''[https://github.com/elij/macher-agent macher-agent]''' || Full, experimental || No || Automatic || Model backend plus sandbox and worktree orchestration || Yes, backend-dependent || Backend-dependent || Sandbox and worktree placement complicates remote use || Project prompts plus backend configuration || Yes; asynchronous subagent orchestration || Young project, smaller ecosystem, and less production history ||
|| '''[https://github.com/FrauH0lle/mevedel mevedel]''' || Partial to editing || No continuous autocomplete; no NES || Project and tool dependent || Uses gptel APIs, directives, and tools || Yes, through gptel || Backend-dependent || Similar concerns to gptel || Directives, reusable instructions, and project state || Agent and workflow support; maturity varies || Broad workbench rather than one standardized agent architecture ||
|| '''[https://github.com/xenodium/agent-shell agent-shell]''' || Backend; usually full || No || Usually automatic, depending on selected agent || Emacs ACP client → ACP server or adapter → real agent or CLI || Yes || Whatever the selected agent supports: subscription login, API key, local model, etc. || Best when the ACP server and agent run where the repository lives || Whatever the backend supports: AGENTS.md, CLAUDE.md, skills, etc. || Backend-dependent || ACP may not expose every slash command, rich TUI feature, hook, authentication flow, skill, or subagent visualization; adapter quality varies ||
|| '''[https://github.com/editor-code-assistant/eca-emacs ECA / eca-emacs]''' || Full || Yes, contextual ghost text; not clearly equivalent to Copilot NES || Automatic, including repository map and workspace context || Emacs client → external ECA server → provider API || Yes || Usually provider API keys or local/OpenAI-compatible models || Server can run near the project, but remote setup must be planned || ECA project configuration, prompts, tools, and MCP || Supported depending on version and configuration || Does not inherit Claude Code, Codex, or Copilot CLI-specific features, subscription integrations, commands, or skills ||
|| '''[https://github.com/manzaltu/claude-code-ide.el claude-code-ide.el]''' || Full || No || Automatic || Runs real Claude Code and augments it through MCP and WebSocket integration || No; Claude only || Claude Code-supported login or subscription, or Anthropic API configuration || Best when Claude Code runs where the repository resides || CLAUDE.md, skills, hooks, MCP, and custom agents || Yes, through Claude Code || Claude-only; no autocomplete; TUI-only features may not map directly into Emacs ||
|| '''[https://github.com/stevemolitor/claude-code.el claude-code.el]''' || Backend; full || No || Automatic || Runs real Claude Code in terminal, comint, eat, or vterm || No; Claude only || Claude Code authentication || Strong when launched on the remote host || Full Claude Code configuration ecosystem || Yes, if exposed by the CLI || Less structured native review and navigation than IDE-oriented clients ||
|| '''[https://github.com/gdanov/emacs-gravity emacs-gravity]''' || Backend; full || No || Automatic || Structured Emacs frontend over Claude Code sessions || No; Claude only || Claude Code authentication || Depends on where Claude Code runs || Full Claude Code configuration ecosystem || Yes; explicitly visualizes agent trees || Newer integration; Claude-only; must keep pace with Claude session structures ||
|| '''[https://github.com/jingtaozf/emacs-claude-agent emacs-claude-agent]''' || Backend; full || No || Automatic || Direct frontend around the Claude Code process || No; Claude only || Claude Code authentication || Backend placement determines remote behavior || Claude Code instructions and configuration || Backend-dependent || Smaller ecosystem; overlaps with other Claude integrations ||
|| '''[https://github.com/cpoile/claudemacs claudemacs]''' || Backend; full || No || Automatic || Manages multiple external agent CLI sessions || Multiple CLIs || Selected CLI authentication || Generally strong when the CLI runs on the remote host || Whatever the selected CLI supports || Backend-dependent; supports multiple sessions || Primarily a session and process manager rather than one deeply structured integration ||
|| '''[https://github.com/dgillis/emacs-codex-ide emacs-codex-ide]''' || Full || No || Automatic || Native Emacs client communicating with Codex app-server || No; Codex only || Codex or ChatGPT login, or OpenAI API, depending on configuration || App-server must run with correct project and filesystem access || AGENTS.md, Codex configuration, and exposed skills or features || Codex-dependent || Newer project; app-server features still need explicit Emacs-side rendering ||
|| '''[https://github.com/benthamite/codex codex.el]''' || Full || No || Automatic || Codex app-server mode or terminal backend || No; Codex only || Codex authentication || Terminal mode is often easiest remotely; app-server placement matters || Codex project instructions and configuration || Codex-dependent; supports multiple sessions || App-server and terminal backends may differ in behavior and available UI ||
|| '''[https://github.com/bennfocus/codex-cli.el codex-cli.el]''' || Backend; full || No || Automatic || Runs real Codex CLI in a project terminal || No; Codex only || Codex authentication || Strong if installed on the remote server || Full CLI-visible Codex configuration || Codex-dependent || Minimal native integration; fewer structured and clickable objects ||
|| '''[https://github.com/copilot-emacs/copilot.el copilot.el]''' || Partial chat and editing; not full CLI Agent Mode || Yes; inline completion and genuine NES || Completion is contextual; agent autonomy is limited || Emacs client → official Copilot language server → GitHub service || Multiple Copilot-hosted models, not arbitrary providers || GitHub Copilot subscription or enterprise seat || Node and the language server need project access || Copilot instructions and skills where exposed || No full coding-agent subagent system || First-party IDE features may appear before language-server or Emacs support; best paired with a separate agent ||
|| '''[https://github.com/chep/gh-copilot-chat.el gh-copilot-chat.el]''' || Partial || No || Mostly explicit buffers, files, and project context || Calls GitHub Copilot chat services || Multiple Copilot-hosted models || GitHub Copilot subscription || Explicit context can work remotely; broader scope may be awkward || Some Copilot and project instructions || No native subagent system || No autocomplete by itself; not a replacement for full Copilot CLI Agent Mode ||
|| '''[https://github.com/MatthewZMD/aidermacs Aidermacs]''' || Full pair-programming agent || No || Automatic repository map plus explicit editable and read-only files || Emacs launches Aider through comint or vterm; Aider calls provider APIs || Yes || Usually provider API billing or local models || Remote support depends on process and repository placement || .aider.conf.yml, conventions, and model or editor settings || Architect and editor split; not a general subagent tree || Configuration precedence and editable or read-only file management can be confusing ||
|| '''[https://github.com/tninja/aider.el aider.el]''' || Backend; full || No || Automatic repository map plus explicit files || Lightweight wrapper around Aider CLI || Yes || Aider or provider authentication || Depends on how Aider is launched || Aider configuration and conventions || Aider-dependent || Usually less polished than Aidermacs; significant overlap ||
|| '''[https://github.com/dnouri/pi-coding-agent pi-coding-agent]''' || Full || No || Automatic || Purpose-built Emacs frontend communicating with the Pi process || Yes || Whatever provider or login methods Pi supports || Pi process should run near the project filesystem || Pi instructions, extensions, and project configuration || Depends on Pi features and extensions || Rich TUI widgets and some interactive commands may not map; login may need an external terminal ||
|| '''[https://github.com/chocoelho/cursor-agent.el cursor-agent.el]''' || Backend; full || No || Automatic || Emacs wrapper around Cursor Agent CLI || Models offered by Cursor || Cursor account or subscription || Best when the CLI runs where the project exists || Cursor rules and CLI configuration || Cursor-dependent || Does not provide Cursor editor autocomplete inside Emacs; tied to the Cursor service ||
|| '''[https://github.com/tninja/ai-code-interface.el ai-code-interface.el]''' || Backend-dependent || No || Backend-dependent || Compatibility layer over multiple packages and CLIs || Yes || Backend-dependent || Backend-dependent || Backend-dependent || Backend-dependent || Adds another abstraction layer; special backend features still need explicit adapters ||
|| '''[https://github.com/milanglacier/minuet-ai.el minuet-ai.el]''' || No || Yes; FIM and chat-model completion; no true NES || Buffer and local context, not autonomous repository search || Emacs calls completion-model APIs directly || Yes || API keys or local models || Usually easier with TRAMP because it mainly uses buffer text || Provider and completion prompt configuration || No || No shell, repository search, multi-file edits, agent loop, or genuine NES ||
|| '''Direct CLI in [https://codeberg.org/akib/emacs-eat eat], [https://github.com/akermu/emacs-libvterm vterm], or [https://github.com/dakra/ghostel ghostel]''' || Backend; full || No Emacs-native autocomplete || Usually automatic || Real CLI subprocess with no normalization layer || Depends on CLI || Whatever the selected CLI supports || Often best for remote servers || Full native CLI configuration ecosystem || Full backend support || Weakest native Emacs integration; keybinding conflicts, TUI rendering, and clickable references may be awkward ||
|| '''[https://github.com/lanceberge/elysium elysium]''' || Editing assistance, not full agent || No continuous autocomplete || Mostly explicit or project-assisted || Built around gptel editing workflows || Yes, through gptel || Backend-dependent || Similar to gptel || Inherits gptel configuration || No || Better classified as editing assistance than autonomous coding ||
|| '''[https://github.com/xenodium/chatgpt-shell chatgpt-shell]''' || Partial or general LLM client || No native NES || Mostly explicit context || Emacs shell-like client calling model APIs || Multiple backends depending on setup || Usually API billing || Remote files are possible; shell and tool routing varies || Prompt and configuration dependent || No native coding subagents || General chat client, not primarily a repository-wide coding agent ||
|| '''[https://github.com/s-kostyaev/ellama ellama]''' || Full agentic coding || No native ghost-text NES; includes explicit code-completion commands || Automatic full-repository scope through project-aware tools and agent loops || Emacs package using the llm library with local and cloud providers; agent loops call project, file, shell, and other tools || Yes || Local models or provider APIs || Project and file tools can operate on the active project; remote behavior depends on provider and tool or process placement || Prompt templates, sessions, configurable tools, and customizable subagent roles || Yes; asynchronous delegation through the task tool with configurable roles, providers, prompts, tool permissions, and step limits || Not a provider CLI frontend, so it does not inherit Claude Code or Codex-specific commands or subscription features; ghost-text completion is not its primary workflow ||
|| '''[https://github.com/rksm/org-ai org-ai]''' || No; full-agent behavior would require extensive customization || No native NES || Explicit Org and buffer context || Org-mode interface to model APIs || Backend-dependent || Usually API billing || Works with Org buffers, including remote files || Org directives and prompts || No || Best for literate chat and content workflows, not autonomous coding ||
|| '''[https://github.com/lizqwerscott/mcp.el mcp.el]''' || Infrastructure only || No || Depends on consuming client || MCP client and tool bridge for Emacs and gptel || N/A || N/A || Depends on MCP server placement || MCP server configurations || Depends on tools || Not an independent coding assistant; should augment another row ||
|| '''[https://github.com/zawatton/anvil.el anvil.el]''' || Infrastructure only || No || Depends on external agent || Exposes Emacs capabilities as an MCP server || N/A || External-agent dependent || Useful when the agent and Emacs can reach each other || MCP-exposed Emacs functions || External-agent dependent || Not an independent coding assistant ||
|| '''[https://github.com/xenodium/agent-review agent-review]''' || Review agent, not primary coding agent || No || Project and Git scope || Uses ACP agents for code review || Yes, through agent-shell and ACP || Backend-dependent || Similar to agent-shell || Backend-dependent || Backend-dependent || Add-on rather than standalone coding environment ||
|| '''[https://github.com/xenodium/emacs-skills emacs-skills]''' || Instructions and skills add-on || No || Depends on consuming agent || Supplies skills and instructions to agents such as Claude Code || N/A || External-agent dependent || Depends on agent placement || Skills files || External-agent dependent || Not a standalone coding tool ||
|| '''[https://github.com/skissue/llm-tool-collection llm-tool-collection]''' || Tool library || No || Depends on consuming agent || Reusable Elisp tools for gptel-style agents || N/A || Backend-dependent || Tool-implementation dependent || Tool definitions || Tool-dependent || Not an independent agent or UI ||

Corrections from package maintainers are welcome.
