Open `M-x ielm RET`. Type the following, `package-archives RET`. Ensure that you have what you expect, for example, elpa.gnu and melpa. It's recommended you make sure they are both `https`. Next, try `(package-archive-refresh) RET`. Next try `M-x list-packages RET`.
