*Please remove this page and tell just how to get this info instead!*


[new]
`w32-toggle-lock-key' - toggle 'Caps Lock', 'Scroll Lock' and 'Num Lock' keys programatically.

<code>
w32-toggle-lock-key is a built-in function in `C source code'.

(w32-toggle-lock-key key &optional new-state)

Toggle the state of the lock key key.
key can be `capslock', `kp-numlock', or `scroll'.
If the optional parameter new-state is a number, then the state of key
is set to off if the low bit of new-state is zero, otherwise on.
</code>
