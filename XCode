[http://developer.apple.com/tools/xcode/ XCode] is an IDE by Apple Inc.

==Emacs Integration==

It is possible to trigger compilation of an XCode project with M-x compile using ''xcodebuild''.  For example:

  xcodebuild -sdk macosx10.5 -configuration Debug build


