'''[::Dired X]''' is the GnuEmacs library ##dired-x.el##. It provides extra functionality for [[DiredMode|Dired]]. It comes with Emacs.

To use this mode run `M-x load-library RET dired-x RET' or add the following to your InitFile.

  (add-hook 'dired-load-hook (function (lambda () (load "dired-x"))))


----
CategoryDirectories
