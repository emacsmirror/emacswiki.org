The Emacs library [::dired-x] provides extra functionality for DiredMode.

It comes with Emacs.

* CVS:dired-x.el

To use this mode run `M-x load-library RET dired-x RET' or add the following to your InitFile.

  (add-hook 'dired-load-hook
	  (function (lambda () (load "dired-x"))))

A manual that comes with Emacs the _extra_ features for Dired Mode for GNU Emacs.  It
is derived from version 1.191 of Sebastian Kremer's `dired-x.el'.



See also DiredPower

----
CategoryDirectories
